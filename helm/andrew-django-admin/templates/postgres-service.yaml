apiVersion: v1
kind: Service
metadata:
  name: {{ .Chart.Name }}-postgres-service
spec:
  selector:
    app.kubernetes.io/instance: {{ .Chart.Name }}
  type: NodePort
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
